workflows:
  android-release:
    name: Android Release Build
    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build appbundle --release

    artifacts:
      - build/app/outputs/bundle/release/*.aab