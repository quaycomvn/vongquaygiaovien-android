name: teacher_wheel_teacher
description: Teacher Lucky Wheel - Flutter Native
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.14

flutter:
  uses-material-design: true